

import React from 'react';

const Error = ( ) => {
  return(
      <div className="error">
        <h2 className="error-message">There are no posts to display</h2>
      </div>
    );
  }

export default Error;
